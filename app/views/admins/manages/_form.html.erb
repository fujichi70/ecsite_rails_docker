<%= form_with(model: admins_manage) do |form| %>
  <% if admins_manage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admins_manage.errors.count, "error") %> prohibited this admins_manage from being saved:</h2>

      <ul>
        <% admins_manage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
