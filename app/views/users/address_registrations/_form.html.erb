<%= form_with(model: users_address_registration) do |form| %>
  <% if users_address_registration.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(users_address_registration.errors.count, "error") %> prohibited this users_address_registration from being saved:</h2>

      <ul>
        <% users_address_registration.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
